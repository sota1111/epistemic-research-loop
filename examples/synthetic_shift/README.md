# Synthetic shift fixture

The executable synthetic benchmark is implemented in `epistemic_loop.benchmark.synthetic`. This
directory intentionally contains no sealed labels; it is safe to fingerprint and mount into a
research worker.
